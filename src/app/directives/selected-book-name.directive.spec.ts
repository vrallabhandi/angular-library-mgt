import { SelectedBookNameDirective } from './selected-book-name.directive';

describe('SelectedBookNameDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedBookNameDirective();
    expect(directive).toBeTruthy();
  });
});
